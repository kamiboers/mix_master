<h1>Songs by <%= @artist.name %></h1>
<% @songs.each do |song| %>
	<div><%=  link_to "#{song.title}", song_path(song) %>
	<%= link_to "Delete", song_path(song), method: :delete, data: { confirm: 'Are you sure that you want to delete ' + song.title + '?' } %>
	</div>
<% end %>